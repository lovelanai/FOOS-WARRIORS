export interface UserProps {
  name: string;
  description: string;
  img: string;
  id: string;
  email: string;
  currentToken: string;
}

export interface NotificationProps {
  title: string;
  text: string;
  time: string;
  id: string;
}
